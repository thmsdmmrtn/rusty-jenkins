# ── Rust / Cargo ──────────────────────────────────────────────────────────────
target/

# Cargo.lock is intentionally committed for binary crates (CLI tools) so that
# builds are reproducible. Remove this line only if you convert this into a library.

# ── Credentials & secrets ─────────────────────────────────────────────────────
.env
.env.*
secrets/

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.iml

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Windows ───────────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
*.pdb

# ── Linux ─────────────────────────────────────────────────────────────────────
*~

# ── Logs & temp files ─────────────────────────────────────────────────────────
*.log
*.tmp
